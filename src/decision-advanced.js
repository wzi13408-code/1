function fromSimulation(sim={}){const c=sim.scoreCounts||[];const scores=[];let total=0;for(let h=0;h<c.length;h++)for(let a=0;a<(c[h]||[]).length;a++)total+=c[h][a];for(let h=0;h<c.length;h++)for(let a=0;a<(c[h]||[]).length;a++)if(c[h][a])scores.push({score:`${h}-${a}`,home:h,away:a,p:c[h][a]/total});scores.sort((x,y)=>y.p-x.p);const top=scores.slice(0,8);const first=top[0], second=top.find(x=>x.home!==first.home||x.away!==first.away)||top[1];return {topScores:top,finalScores:[first,second],result:sim.result||{}}}
function consistency(d){const r=d.result||{};const score=d.finalScores?.[0];const implied=score?score.home>score.away?'home':score.home<score.away?'away':'draw':null;const max=Object.entries(r).filter(([k])=>['home','draw','away'].includes(k)).sort((a,b)=>b[1]-a[1])[0]?.[0];return {consistent:!implied||!max||implied===max,scoreResult:implied,resultModel:max}}
function antiBias(d){const warnings=[];const r=d.result||{};if(Math.max(r.home||0,r.away||0)>0.72)warnings.push('strong-favorite concentration');if((r.home||0)+(r.away||0)>0.9)warnings.push('draw underweight check');return {warnings,penalty:warnings.length*0.01}}
function decide(sim){const base=fromSimulation(sim);return {...base,consistency:consistency(base),antiBias:antiBias(base)}}
module.exports={decide,fromSimulation};
