# XT_FINAL_SYSTEM 项目核心规则

> 项目：🔥 XT终极足球竞彩系统
> 核心规则文件：`XT_FINAL_SYSTEM.md`

## 强制规则

`XT_FINAL_SYSTEM.md` 是本项目的**唯一核心分析规则与行为基准**。所有后续足球竞彩分析、预测模型、代码模块、接口、模拟器、赔率模块、冷门识别、串关生成器以及输出格式，都必须以该文件为最高业务规则来源。

### 执行优先级

1. `XT_FINAL_SYSTEM.md`：核心业务规则，最高优先级。
2. 代码中的实现逻辑：必须服从核心规则；如与核心规则冲突，应修改代码而不是修改规则含义。
3. 数据源/API：用于提供证据，不得覆盖核心规则。
4. 用户临场输入：可用于更新模型，但必须按照核心规则重新分析。

### 核心要求

- 必须先完整分析，再决定最终比分。
- 不得因为“求稳”机械压低比分。
- 每场最终严格输出两个比分，并由完整剧本竞争产生。
- 必须考虑基本面、阵容与首发/替补对位、近期状态、主客场、xG、战术、赔率盘口、市场热度、资金变化、冷门、0-0、高比分、强队屠杀、强队输弱及非线性事件。
- 每场至少进行10万次蒙特卡洛/情景模拟的设计与实现要求；不得只使用传统低比分分布。
- 临场阵容、赔率、盘口和市场变化必须能够推翻早期结论并重新计算。
- 冷门不能机械制造；热门也不能机械追捧。
- 证据 > 模板；比赛结构 > 固定比分；完整分析 > 主观感觉。

## 开发约束

新增或修改任何 XT 模块时，先检查 `XT_FINAL_SYSTEM.md` 是否已有对应规则。若已有，代码必须与其保持一致；若发现冲突，应优先修正实现。

任何比分、胜平负、让球胜平负、进球数、半全场、大小球、串关和混合组合生成逻辑，都属于 XT 核心规则的下游实现，不得自行建立与 `XT_FINAL_SYSTEM.md` 相冲突的“旧XT”逻辑。

## 规则更新

今后若用户明确要求修改 XT 核心规则，应直接更新 `XT_FINAL_SYSTEM.md`，并同步检查受影响的代码模块与文档，确保项目实现与规则一致。
