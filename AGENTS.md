# XT_FINAL_SYSTEM 项目核心规则

> 项目：🔥 XT终极足球竞彩系统
> 核心规则文件：`XT_FINAL_SYSTEM.md`
> 当前规则版本：XT 3.0 FINAL

## 强制规则

`XT_FINAL_SYSTEM.md` 是本项目的**唯一核心分析规则与行为基准**。所有后续足球竞彩分析、预测模型、代码模块、接口、模拟器、赔率模块、冷门识别、串关生成器以及输出格式，都必须以当前版本的该文件为最高业务规则来源。

### 执行优先级

1. `XT_FINAL_SYSTEM.md`：核心业务规则，最高优先级。
2. 代码中的实现逻辑：必须服从核心规则；如与核心规则冲突，应修改代码，而不是反向改变规则含义。
3. 数据源/API：负责提供证据，不得覆盖核心规则。
4. 用户提供的临场信息：属于高价值输入，必须按照核心规则重新分析。

### 当前 XT 3.0 核心要求

- 必须先完整分析，再决定最终比分。
- 禁止先定比分再反向寻找理由。
- 不得因为“求稳”机械压低比分。
- 不得看到热门就机械反买，也不得看到强队就默认获胜。
- 不得因为高比分概率低就删除高比分尾部。
- 不得因为0-0概率低就删除0-0。
- 每场最终严格输出两个比分，且必须来自完整候选比分/比赛剧本竞争。
- 必须考虑基本面、阵容与确认首发/预计首发、替补及位置对位、近期状态、主客场、xG/xGA、战术、赛程、战意、赔率盘口、市场热度、资金变化、冷门、0-0、高比分、强队屠杀、强队输弱、早球与红牌等非线性事件。
- 必须建立主客队预期进球 `λ_home`、`λ_away`、`λ_total`，并根据阵容、战术、赛程、市场及数据可信度进行修正。
- 每场至少进行10万次蒙特卡洛/情景模拟的设计与实现要求；如果实际程序未运行，不得声称已经完成真实10万次模拟。
- 临场阵容、赔率、盘口、大小球及市场变化必须能够推翻早期结论并触发重新计算。
- 冷门不能机械制造；热门也不能机械追捧。
- 数据缺失时不得编造；必须区分确认事实、模型推断与主观判断。
- 数据可信度本身必须进入模型/结论权重。
- **证据 > 模板；比赛结构 > 固定比分；完整分析 > 主观感觉。**

## 开发约束

新增或修改任何 XT 模块时，必须先检查 `XT_FINAL_SYSTEM.md` 是否已有对应规则。若已有，代码必须与其保持一致；若发现冲突，应优先修正实现。

任何比分、胜平负、让球胜平负、进球数、半全场、大小球、冷门识别、赔率分析、10万次模拟、串关和混合组合生成逻辑，都属于 XT 核心规则的下游实现，不得自行建立与 `XT_FINAL_SYSTEM.md` 相冲突的旧版逻辑。

## 规则更新机制

今后用户重新优化或明确修改 `XT_FINAL_SYSTEM.md` 时：

1. 以最新 `XT_FINAL_SYSTEM.md` 内容为准，不沿用旧版 XT 规则。
2. 同步检查 `AGENTS.md` 与受影响代码模块。
3. 如果旧代码与新规则冲突，优先修改代码实现。
4. 不得为了保持旧预测结果而牺牲新规则。
5. 每次核心规则升级后，应通过 Git commit 留下明确的规则升级记录。

## 最终原则

**XT_FINAL_SYSTEM.md 是规则源，代码是实现层，数据是证据层，预测结果是规则与证据共同计算后的输出。**
