# XT_FINAL_SYSTEM 项目核心规则

> 项目：🔥 XT终极足球竞彩系统
> 核心规则文件：`XT_FINAL_SYSTEM.md`
> 当前规则版本：**XT 4.0 CORE**

## 强制规则

`XT_FINAL_SYSTEM.md` 是本项目唯一核心分析规则与业务行为基准。所有足球竞彩分析、数据处理、预测模型、模拟器、赔率模块、阵容模块、战术模块、冷门识别、玩法派生、串关优化、回测和报告输出，都必须服从该文件。

### 执行优先级

1. `XT_FINAL_SYSTEM.md`：最高业务规则。
2. 模型/代码实现：规则的技术实现，不得反向改变规则含义。
3. 数据源/API：提供证据，不得覆盖规则。
4. 用户临场输入：高价值新证据，必须触发重新计算。
5. 赛后结果：只能进入回测与校准，绝不能污染原赛前预测。

## XT 4.0 核心要求

- 必须先完整分析，再决定最终两个比分。
- 必须允许强队0球、强队被零封、热门输球、0-0、高比分、大胜和极端尾部。
- 禁止先定比分再寻找理由。
- 必须使用多模型思路：Elo/xG-Elo、Poisson、Dixon-Coles、Bivariate/Negative Binomial、Bayesian、机器学习、市场模型、阵容/战术模型和情景模拟，在数据允许时进行集成。
- 必须考虑数据质量、防泄漏、球队实力、近期状态、主客场、xG/xGA、事件价值、球员状态、预计/官方首发、逐位置对位、替补、战术、节奏、赛程、战意、天气、裁判、赔率、盘口、市场、冷门、0-0、BTTS、零封、大胜、屠杀、状态转移和非线性事件。
- 每场模拟最低设计目标100,000次；没有真实执行时严禁虚构模拟结果。
- 所有玩法必须从同一底层比分概率分布派生，避免比分、胜平负、进球数、半全场和串关互相矛盾。
- 临场官方首发、赔率、盘口和市场变化可以推翻早期预测。
- 必须保存预测版本和赛前数据快照。
- 必须进行walk-forward/rolling回测、Brier、Log Loss、校准和错误归因。
- 数据不足时必须扩大不确定性，而不是编造信息或假装高置信度。

## 开发约束

新增或修改任何 XT 模块前，必须检查 `XT_FINAL_SYSTEM.md` 对应规则。

任何代码如果与 XT 4.0 冲突，优先修改代码实现，而不是为了保留旧逻辑修改核心规则。

旧XT逻辑不得通过隐藏参数、兼容层或默认值继续影响新系统。

新增模型必须：
1. 有明确输入与输出；
2. 有时间有效性；
3. 无数据泄漏；
4. 有独立回测；
5. 能证明增量价值；
6. 能被Ensemble统一管理；
7. 能在必要时被降权或淘汰。

## 规则更新机制

每次 XT 核心规则升级：
1. 更新 `XT_FINAL_SYSTEM.md`；
2. 同步更新 `AGENTS.md`；
3. 检查所有受影响代码模块；
4. 删除/重构冲突的旧逻辑；
5. 保存明确 Git commit；
6. 对关键模块执行回测和一致性检查。

## 最终原则

**规则是XT的宪法；代码是实现；数据是证据；模型是工具；概率是中间产物；最终两个比分是决策输出；回测是唯一持续改进依据。**
